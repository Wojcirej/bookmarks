<aside class="menu col-2">
  <nav>
    <ul>
      <% @categories.each do |category| %>
        <li style="padding-left: <%= category.depth * 15 %>px;">
          <%= link_to "#{category.name}", category_path(category) %>
        </li>
      <% end %>
    </ul>
  </nav>
</aside>